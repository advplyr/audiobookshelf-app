package com.audiobookshelf.app

/**
 * Re-use the same receiver app ID everywhere so cast scanning and session creation stay in sync.
 */
object CastConstants {
  const val RECEIVER_APPLICATION_ID = "FD1F76C5"
}
